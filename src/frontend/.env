# Local Development Environment Configuration
# Used when running: npm run dev

# API URL - for local development, use localhost:5000
# Note: Service files already include /api/ prefix, so this is just the base URL
VITE_API_URL=http://localhost:5000

# SignalR Hub URL - full URL for local development
VITE_HUB_URL=http://localhost:5000/hubs/checklist

# Enable mock authentication (no real auth for POC)
VITE_ENABLE_MOCK_AUTH=true
